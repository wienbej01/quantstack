# REPORTING

- Read only the artifacts under `runs/<run_id>` and `experiments/<exp_id>`.
- Leaderboards with bootstrap confidence intervals; U‑test p‑values for A/B.
- Portable outputs: Markdown or HTML. Never reach back into the lake.
