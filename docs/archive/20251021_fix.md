# 20251021 Fix Sprint Plan — Regime Pilot Trade Generation

## 0. Guardrails
- Modify only existing project files (e.g. `test_regime_pilot.py`, `qx_features`, `qx_backtest`).
- Respect current interfaces, schemas, deterministic calculations, and 100-character line limit.
- All logging must remain gated by a `verbose` flag (default silent).
- After each workstream, execute indicated quality checks.

---

## 1. Re-enable Enhanced Feature Pack
1.1 In `test_regime_pilot.py::prepare_features`, call `compute_all_regime_enhanced_features(df, config={"verbose": False})` after core and regime packs.  
1.2 Update status message to “Enhanced features computed”.
1.3 Ensure import: `from qx_features.regime_enhanced import compute_all_regime_enhanced_features`.
1.4 When verbose diagnostics run, confirm at least one enhanced column (e.g., `f__anchor__session_avwap`) exists.  
**Quality Check:** `pytest tests/test_regime_enhanced_features.py -q`.

---

## 2. Fix Policy Regime Gating
2.1 Set policy map keys to match `Policy.strategy_id` values (`"avwap_momentum"`, `"avwap_pullback"`, `"value_rotation"`).
2.2 Create readable label map for printouts (`{"avwap_momentum": "Momentum", ...}`).
2.3 Pass strategy map to `BacktestConfig` using those IDs:  
```python
strategy_map = {
    "BULL": ["avwap_momentum", "avwap_pullback"],
    "BEAR": ["avwap_momentum"],
    "SIDEWAYS": ["value_rotation"],
    "STRESS": [],
}
```
2.4 Ensure `regime_config` enables detector with default params (expecting enhanced features).  
**Quality Check:** `pytest tests/test_regime_aligned_policies.py -q`.

---

## 3. Use Engine Result API Correctly
3.1 After `engine.run(...)`, gather outputs via `result.trades_history`, `result.orders_history`, and `result.equity_curve`.  
3.2 Compute `final_equity` safely (`result.equity_curve['total_equity'].iloc[-1]` when available, else `engine.portfolio.total_equity`).
3.3 Calculate `final_return = final_equity - config.initial_cash` and print summary using label map.
3.4 Remove references to nonexistent `result.orders`.  
**Quality Check:** run `python test_regime_pilot.py` (ensuring no AttributeError).

---

## 4. Guarantee Detector Inputs
4.1 After feature prep, assert presence of regime columns:
```python
required = [
    "f__regime__var_ratio_10_60",
    "f__regime__adx_proxy_14",
    "f__regime__band_pos_20_2.0",
    "f__regime__mod_vol_30",
    "f__regime__stress_10_10",
]
```
Raise `RuntimeError` if missing.
4.2 Modify diagnostics to sample detector output directly (call `create_default_detector().evaluate` on grouped bars) and print distribution only when `verbose` flag true.  
**Quality Check:** `pytest tests/test_regime_detector.py -q`.

---

## 5. Add Pilot Smoke Test
5.1 Create `tests/test_regime_pilot_smoke.py` with deterministic synthetic data (`np.random.seed(0)`).
5.2 Steps:
- Run `prepare_features`.
- Assert regime columns present and detector produces at least one BULL/BEAR signal in warmed bars.
- Instantiate `AVWAPMomentumPolicy`, attach to `BacktestEngine`, call pilot strategy helper or replicate logic, and verify `len(result.orders_history) > 0`.
5.3 Keep runtime fast (≤1 s).  
**Quality Check:** `pytest tests/test_regime_pilot_smoke.py -q`.

---

## 6. CLI & Diagnostics Refinement
6.1 Add `argparse` to `test_regime_pilot.py` for `--verbose` flag (default False).  
6.2 Gate all diagnostic prints behind `if args.verbose`. Keep core status prints succinct.
6.3 At summary, include counts of BULL/BEAR bars obtained from diagnostic step.

---

## 7. Final Validation
7.1 Run combined tests:  
`pytest tests/test_regime_enhanced_features.py tests/test_regime_aligned_policies.py tests/test_regime_detector.py tests/test_regime_pilot_smoke.py -q`
7.2 Execute `make lint` and `make check-types`.
7.3 Run pilot script both default and verbose:  
`python test_regime_pilot.py` and `python test_regime_pilot.py --verbose` — confirm non-zero trades/orders and clean output.

---

## Deliverables
- Updated `test_regime_pilot.py` with full feature stack, correct strategy IDs, and CLI flag.
- Smoke test safeguarding regime-to-policy pipeline.
- Passing lint/type/test checks.
- Pilot command producing trades with informative summaries.
