[project]
name = "qx-cli"
version = "0.1.0"
description = "Orchestration CLI"
requires-python = ">=3.11"
dependencies = ["qx-core", "typer", "rich", "pandas", "pyarrow", "pyyaml>=6.0"]
[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"