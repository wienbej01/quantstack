[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "qx-screener"
version = "0.1.0"
description = "Universe selection and screening for QuantStack"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.0.0",
    "numpy>=1.24.0",
    "pyarrow>=12.0.0",
    "pydantic>=2.0.0",
    "qx-core",
    "qx-data",
    "qx-features",
]

[project.optional-dependencies]
test = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/qx_screener"]